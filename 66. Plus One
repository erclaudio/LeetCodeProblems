class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        if digits[-1] != 9:
            digits[-1]+=1
            return digits
        else:
            num = ''.join(map(str, digits))
            num = (str(int(num) +1))

            return(int(char)for char in num)
            
            
        
